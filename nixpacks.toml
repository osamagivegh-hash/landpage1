[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = [
  "cd backend && npm install --only=production"
]

[phases.build]
cmds = []

[start]
cmd = "cd backend && npm start"

[variables]
NODE_ENV = "production"
